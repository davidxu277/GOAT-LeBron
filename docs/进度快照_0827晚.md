# 进度快照 · 08-27 深夜（token 告急时紧急保存）

## 已完成且已推送（全在 origin/main，最新 9eb924d + 本地合并）
- 四角色循环 + 外层 run_session + 三账本（Time/Prior/Shelf）+ 干预记录 + finalize/restore/快照/叙事
- 真执行器（分片parquet/配置驱动/数组字段安全处理）+ 预检 + 网页控制台（自主迭代模式/Finder选文件/实时思维链）
- 锁定集 R3 落地：public_test 只许读一次，读第二次抛错；结果表报泛化落差
- 26 张卡、96 个测试全绿、DeepSeek 通路（流式+截断检测，工兵 96k 上限）

## 实测数字（写报告用）
- 基线 14 特征：点击 AUC 0.5501（validation 21.8万行/38转化）
- 加交叉字段 508/509/702：0.5576（+0.0075，超噪声带）——配置里还没默认启用，留给 Agent 自己发现
- 锁定集 public_test（218万行/467转化）：点击 0.5941 / 购买 0.5628，泛化落差 -0.0365（负=没过拟合开发集，开发集读数噪声大）

## 关键决定（已定，别再议）
- ⛔ public_test 数据**不许**挪进 train：源自官方测试切分（manifest: source_public_test=data/sample_test），混入训练疑似违反比赛唯一硬规则，且推翻 manifest 里 public_test_used_for_training:false 的声明，还拆掉唯一锁定集
- ✅ 转化样本太少的正解：请成员1 把 train/validation 从 9:1 重切成 8:2，或采样率 5%→10-20%（都在官方训练切分内，合规）

## 正在进行
- 六维度多代理审查（第一次因断网全灭，已重启）。结果会落在 workflow 任务输出；确认的问题按严重度处理
- 尚未做：完整多轮真实迭代（条件已齐：run --rounds N + 真数据 + DeepSeek）；军师数字摘要；提示词第二轮迭代

## 明天优先级
1. 跑一场完整多轮迭代（产出交付物 #3/#5）
2. 处理审查确认的问题
3. 成员1 重切数据；NISE 基线复现回填
4. finalize + README 局限性 + Devpost 文案
