# 开发日志

> 规矩：**每条一行**，格式 `月-日 时:分 · 谁 · 做了什么（为什么/结论）`。
> **按时间倒序，最新的在最上面**；带时分是因为一天内两人各插十几条，只写日期分不出先后。
> 时间以 git 提交时间为准（`git log --date=format:"%m-%d %H:%M"`），有疑问就以它为准重排。
> 写不下就说明该拆成两条。详细内容进 `docs/` 下对应文档，这里只留线索。

## 09-01

- 04:10 · 成员3 · **Agent 写代码的能力被一行配置关掉了 5 小时没人发现**。08-31 18:30 我把 `goat_trainer`（FeatureOp/ModelOp/TrainOp 加载 + 深度训练路径）接进 `kuairand_task.yaml`；23:39 `5d16d9e` 把那一行换成了 `official_fm_trainer.py` —— 它只认 6 个超参数，任何带 new_files 的提案当场拒、整轮作废。真跑日志里的代价：第 14~18 轮全废，军师连烧 5 轮才自己摸出这条约束，每一轮是一次真训练加四次大模型调用。**这行改动是静默的**：跑得起来、有分数、日志正常，唯一区别是 Agent 从「能做研究」退化成「调参器」。改法是**拆成两份**，两个需求各有入口谁也不挡谁：`kuairand_task.yaml`（正式跑，goat_trainer，output/agent_run）＋ `fm_baseline.yaml`（基线复现，official_fm_trainer，强制校验第 0 轮，Stephen 那套原样保留）

- 04:10 · 成员3 · 新增 `tests/test_configs.py` 守着这类「改一行就把整个能力关掉」的地方：正式跑那份的 trainer 必须自己声明 `AGENT_SUPPORTS_NEW_FILES = True`；基线那份必须 `require_baseline_reproduction: true` 且不许让 Agent 加零件；两份不许写同一个 output 目录（FM 和深度 MLP 的 best_report/快照/补丁历史会互相覆盖，而那正是「最终交哪一版」依赖的东西）；官方基线数字两份都得对得上。实测把 trainer 换回去当场红

- 03:30 · 成员3 · **军师一直在对着一份假病历开药**（读真跑日志发现）：`goat_run` 喂给军师和工兵的「当前流水线」读的是 `configs/pipeline.yaml` —— 那是**另一个文件**，跟真正在跑的 `trainer_config` 毫无关系。那份写着 `model: item_popularity, prior: 20.0`，而真跑的是官方 FM（k=16, lr=0.001, epochs=40）。于是军师整轮整轮地推理一个根本不存在的模型：该不该给 item_popularity 上 SWA、prior 能不能调到 200 —— 那个键在 FM Trainer 里压根没有，提上去必被拒。而且它还是**静态**的：`effective_config()` 只认执行器的 `config` 属性，bridge 那个执行器没有，于是每轮看到的都是同一份初始文本，工兵改动被接受了也不更新。改法：执行器加 `effective_trainer_config()`（初始 trainer_config + 历轮 config_patch 深合并），`_render_pipeline()` 从真实来源渲染，`effective_config()` 优先认它

- 03:30 · 成员3 · 同批：**把「这一场能不能写代码」直接告诉军师**。真跑日志里它连烧 5 轮才自己摸出「带 new_files 的提案会被当场拒、整轮作废」—— 每一轮是一次真训练加四次大模型调用。这是**已知事实**，不该让它花钱试出来。两个 Trainer 各自声明 `AGENT_SUPPORTS_NEW_FILES`，执行器 `agent_capabilities()` 读出来，连同可改子树和参数取值范围一起写进流水线文本。顺带在 `official_fm_trainer` 那句 NotImplementedError 旁边写明：**这是我们这份文件的限制，不是主办方的规定** —— 赛题恰恰奖励「超越简单基线调参」，那句话里的「官方」指的是官方 baseline 那个 FM 模型，不是官方规则

- 02:10 · 成员3 · **人工干预记了也白记**（同学那边的 Claude 查出来的，我复现确认）：`agent.cli intervene` 写死仓库根 `logs/interventions.jsonl`，而每一场的 `logs_dir` 各自独立 —— bridge 那条路是 `kuairand_goat_bridge/output/<场次>/logs/`，离线演习是 `logs/offline/`，只有 AliCCP 老路径恰好对得上。实测：记了一条，bridge 那一场 `drain()` 拿到 **0** 条。于是交付物 #3 里那个「人工干预 N 次」在我们**实际要跑的那条路径上**永远是 0 —— 而 `loop.py:189` 的注释自己写着「一个只能是 0 的数字，评委翻一眼代码就知道不算数」。改法：`InterventionLog` 收多个路径，每一场除了自己的日志目录**再盯一份仓库根的** logs/（同一份路径去重，不重复计数）；`intervene` 加 `--logs`，并打印真实写入路径。人在跑的过程中插手那一刻正忙着处理问题，不该还要先想「这一场的日志目录在哪」

- 01:20 · 成员3 · **「交上去的不是最好那一版」这个洞，5d16d9e 的修法本身有三个错，全都跑到最后才炸**。方向是对的（Agent 轮次 ≠ 执行器轮次，必须让成绩单自己带编号），但：① `best_report["执行器轮次"]` 取错了嵌套层 —— 那个字段写在「运行预算」下面，顶层读是 KeyError，而这一行在 `run_session` **之后**，整场（最长 6 小时）训练跑完、就差写提交文件时才炸；② `make_final_submission` 那个 `_health_report` 调用点漏传新加的 `executor_round`，TypeError，同样发生在最后一步；③ `selected = self._selected_round if self._selected_round else 末尾` —— **第 0 轮是假值**，会被当成"没选过"退回末尾那一轮，Agent 一轮都没改进时最该交的恰恰是第 0 轮，于是又变回交错版本。另外那次提交时 bridge 套件是红的（3 处测试没跟着补参数）

- 01:20 · 成员3 · 三条都修了，各配一条会红的测试。第 ③ 条特别说明：原有的 `test_final_submission_consumes_attempt` 盖不住它 —— 那个测试 history 里只有一轮，末尾恰好也是 0，两条分支结果一样。新测试跑够两轮才分得出来（把 bug 放回去当场红：`1 != 0`）。取不到执行器轮次时改成**当场报错**并说清原因，不再默默往下走 —— 下一步就是生成提交，宁可在这里停，也不能交一个不知道是哪一轮的版本

## 08-31

- 23:45 · 成员3 · **官方基线不再进 Agent 的视野**（成员3 坚持的口径）。赛题按「相对官方基线的提升」排名 —— 那是**评委的尺子，不是 Agent 的输入**。给了它，Agent 会退化成对着一个固定数字调参：超过就判"没病"、低于就笼统报"学得不够"，而不去看训练/验证差、分桶、用户构成这些真正指得出病因的证据；而且这个数字一旦写错，错法是**静默**的（22:10 那条就是活例子）。做法三层：① `_health_report` 不再产出「官方Validation基线」「相对官方基线」两块；② `roles.strip_baseline()` 在医生和复盘官的提示词入口再兜一道，带「基线」字样的块一律摘掉；③ 病名表里「学得不够」「Top5排不上去」改成**只用 Agent 自己够得着的信号** —— 学得不够 = 训练集自己也没学好（训练/验证差 < 0.02）且最佳轮次等于最后一轮；Top5 = 两个指标相对**上一版**朝相反方向动。基线仍然全程记录，在 `final_summary.json` 的 official_baseline_validation / baseline_reproduction 两块里，人这边该看的对比一个不少。测试锁死：成绩单原文里出现 0.6674/0.5357/0.6016/0.6610/0.5946 任何一个就红

- 23:45 · 成员3 · **复盘官被要求填的指标名跟数据对上了**。换数据集之后成绩单里是 GAUC / nDCG@5，而 `schemas.METRICS` 还写着「点击AUC / 购买AUC」—— 复盘官被要求填两个数据里根本不存在的字段，填出来的数还会拿去跟噪声带比、拿去更新卡片信任分。这种错不报错，只安静地污染账本。改法：`METRIC_PAIRS` 一张表当唯一来源（成绩单字段名 → 对 LLM 报的指标名），`metric_names(report)` 按成绩单认任务，医生 affects / 军师 expected / 复盘官 actual / `read_scores` / 崩溃时合成的复盘全部走同一套；AliCCP 那两个名字原样保留，旧测试不动

- 23:45 · 成员3 · 顺带三条：① 医生提示词改成指标中立（「点击 AUC」→「主分」），新增第 4 条硬规矩「没有官方基线，别去找它」—— 截图里医生为一个不存在的基线纠结了整整两大段，还自己编了个数；② 噪声带的指标名跟当场成绩单对不上时**写进结果表**（以前是 `bands.get(k, 0)` 静默退回 R11 兜底，结果没错但"我们量过带子了"这个印象是错的）；③ `goat_trainer.apply_agent_patch` 补丁应用前先 `cleanup_agent_patch()` —— 覆盖配置整份重置而生成文件不重置，会出现"上一轮的零件还在磁盘上、`_GENERATED_FILES` 已经空了"，history 重放到同一路径被当成"想覆盖用户代码"拒掉

- 22:10 · 成员3 · **医生终于看得见证据了**（真跑一场之后才暴露的两类错）。① 病名表里的官方基线数字**写错且把结论写反**：「学得不够」写着"官方基线 GAUC 0.6016"，可 0.6016 是 **primary**，GAUC 真值是 0.6674。那一场跑出 GAUC 0.6638（**低于**基线 0.0036，三个指标全部低于基线），医生拿着错尺子算出"明显高于基线"，把唯一该报的病判成了没病。现在基线一律从成绩单的「官方Validation基线/相对官方基线」读，**不写死在病名表里** —— 写死的常数走岔时不报错，只会安静地带偏结论。② 12 个病里 **6 个的判据成绩单里压根没有**：新建 `kuairand_bridge/diagnostics.py` 补齐训练集自评（在背题）、视频曝光分桶（冷门视频）、见没见过用户分堆（新用户）、用户构成（退化用户）、逐日分段（时间漂移）、预测健康（数据对不上），全部摊平到成绩单顶层跟「验证集」平级。R2：分桶依据只从 train 统计；分组指标一律调官方 `evaluate`，不自己实现

- 22:10 · 成员3 · 同批三个配套修复：① 危险信号闸门（训练/验证差 >0.15）原来只读「点击分」，KuaiRand 成绩单没有这个字段 —— 那场真跑它**一次都没响过**，等于不存在；改成按主指标名配对取，两边指标名对不上就不算。② `read_scores` 同样只认「点击分」，b35cbff 删掉兼容字段后一路返回空 dict：收敛判定走「主分」不受影响，但 `best_scores`、锁定集分数、叙事里每轮的分数全是空的，交付物 #3/#5 上直接看得见。③ 分组证据带「分组口径提醒」—— 实测小份上验证集总主分 0.5800、按日期切开每天只有 0.50~0.53，看着像暴跌其实一天没跌（nDCG@5 是用户内指标，数据切小之后每个用户曝光条数变少，天然偏低）。没有这句，医生会拿分组分数比总分，每轮报一个不存在的「时间漂移」

- 22:10 · 成员3 · 小份真数据实测新证据块，两个病当场现形：**在背题** —— 训练集主分 0.6456 vs 验证集 0.5800，差 0.0656（门槛 0.04）；**退化用户占比高** —— GAUC 参与用户只占 57.8%（门槛 0.6），零正例用户 30.3%。另外两条是"排除"也同样有价值：冷门视频桶 GAUC 0.6214 vs 最热桶 0.6286，只差 0.007，不是病（nDCG 差 0.157 是分组样本量效应，不能当证据）；没见过的用户 GAUC 0.6449 反而**高于**见过的 0.6358

- 20:25 · 成员3 · 修审查报告前四条：**目标编码泄漏**（零件写好了折外通道 `transform_train_with_fold`，但加载器一直调普通 `transform` —— 只出现一次的 ID，编码值精确等于这一行自己的标签）；目标列默认 `ctr_label` 那一列根本不存在；折分种子写死 42（跑多种子时这部分随机性被抹掉）；三个零件补 `needs()`（不声明会退化成读整表）；MLP 三个参数改必填（默认值最坑的不是值不对，是掩盖配置拼写错误）

- 18:30 · 成员3 · **把 AliCCP 那套能力全接到 KuaiRand 上** `examples/goat_trainer.py`：FeatureOp/ModelOp/TrainOp 加载、R5 路径守卫、R2 只在 train 上 fit、embedding 表 + epoch 循环 + 最佳权重回滚 —— 全部复用 `harness/`，trainer 只是把 KuaiRand 的行元组翻译成它们吃的 DataFrame。`train_deep` 加 `task_loss`/`task_metric` 两个钩子脱离具体任务（默认行为不变）。真数据实测：120 万行训练集切 12 万跑 2 轮，primary 0.5534→0.5663；模拟工兵写「视频热度分桶」零件，新列自动进特征表、分数跟着变

- 18:16 · 成员3 · **解除硬阻塞**：`official_fm_trainer.py` 补上 `apply_agent_patch` —— 在这之前工兵只要产出任何补丁，Bridge 就抛 NotImplementedError，整轮作废，这条路一轮都跑不成。补丁累积重放（每轮从初始配置重叠 history），只准动 model/train 两棵子树，六个参数各有区间护栏（epochs 一个 500 就能烧光整场预算），越界**报错而不是静默夹回** —— 让复盘官知道这是「提案不合法」不是「方法没用」。顺带删掉 `cards/retired/` 17 张

- 18:12 · 成员3 · **知识库迁到 KuaiRand**：病名 12→12（删 4 个漏斗病、冷门商品学不动→冷门视频排不上去、新增 Top5排不上去/排序损失不匹配/时间漂移/退化用户占比高）；卡片 26→14（17 张多任务/CVR 去偏卡退休进 `cards/retired/`，5 张改标签，搬 bridge 的 pairwise_loss+popularity_prior，新写时间衰减/用户内归一化/对齐自检 3 张），12 个病全部有药。删掉 `goat_profile/`，`goat_run` 改读 `knowledge/` —— 两份并存的代价是改一边忘一边，军师看到的永远是没人维护的那份

- 12:37 · 成员3 · 清掉 `modules/features/demo_op.py` —— 那是离线演习的假工兵写出来的，被我在 333c9f6 里 `git add -A` 顺手带进仓库，跟真零件混在一起。假工兵改用 `_offline_scratch` 前缀，`.gitignore` 加 `modules/**/_offline_*.py`，以后演习产出溜不进来

- 12:35 · 成员3 · **修 main**：`f15ccef` 把 7 处冲突标记原样提交了，仓库从 192 绿变成连 import 都不行。两个父提交都是成员4 自己的分支 —— `4bd5081` 含成员2 最新 main（单独跑 192 绿）、`ea775fc` 有 8 个新零件和 CUDA（不含 main）。解法：4 个共享文件整页取 4bd5081（冲突处两边是同一功能的两套实现，那套多一道内存守卫且有测试），10 个新文件原样保留，再把只在 ea775fc 有的 `read_training_sample`、`_HAIKU_45_MAX_OUTPUT_TOKENS`（改成从上限表派生）、`_schema_for_anthropic` 搬回来。
- 12:35 · 成员3 · 顺带救回 `DANGEROUS_TRAIN_VAL_AUC_GAP`（训练/验证 AUC 差 >0.15 时医生不许说没病、严重度不得低于 0.7）—— 它藏在被整页替换的文件里、又没有测试盖着，差点无声丢掉。补了两条测试锁住，下次谁再丢就会红

## 08-29

- 00:20 · 成员2 · **Agent 自主迭代第一次真正成功**（medium 训练 + 归档验证，10 轮 0 干预）：购买 AUC 0.5563→0.5780（+0.0217，噪声带 0.01945）、点击 0.5519→0.5559（+0.0040，带 0.00274），两项都越过各自的带子。它的诊断是「在背题」，药是把高基数 ID（101/205/206/207/301）从特征里撤掉、换成工兵自己写的频次编码零件，再压两塔容量——机制说得通，第 8 轮还主动只动购买塔、留点击塔做不变对照。前一场同样 10 轮 0 成功，差别就是「失败原因传不传得下去」：这一场五个自创方案里三个明确引用了前面某轮的具体报错并绕开
- 00:20 · 成员2 · 修工兵写模型零件的三处契约错（`_EXAMPLES` 没有「模型」档→兜底给了训练类范文；`ModelOp` 没声明 `__init__(config)` 而加载器用 (config) 实例化；契约写 `predict(model, df)` 而实际传的是编码后的 LongTensor）——这是 21 张模型/损失卡落不了地的直接原因
- 00:05 · 成员2 · 单值的多值列在 Arrow 层就压成标量：pandas 里一个多值列是「每行一个 Python 对象」，光对象头就约 110B/行，跟装几个元素无关。全量上 508/509/702 从 12.7G 降到 0.8G，这三个字段现在全量可用（开发日志 08-27 实测过它们 +0.0075）。内存守卫补上验证集那一侧——它可能比训练集还大
- 23:30 · 成员2 · 把「为什么没跑起来」传给下一轮：`_crashed_reflection` 的 `next_hint` 原来写死一句「换一个方案」，它非空导致 `_brief` 里 `or` 后面的 recoveries 永远轮不到，执行器的报错在这一步被扔掉；自创方案 `card_id` 是空串，`if card_id:` 整段跳过，也不进「已经试过的」。军师甚至根本收不到 history_brief
- 22:00 · 成员2 · 爬山回滚：以前是贪心累加，判「猜错了」只拉黑卡片、改动本身留在流水线上，搜索轨迹一去不回头。门槛做成 `--rollback-margin` 旋钮

## 08-28

- 16:40 · 成员2 · 噪声带对不对得上这一场的档位，从"要靠人记得"变成"系统自己说"：① 分桶的实测带子**恒等于 0.0000**（保真度抽样只抽负样本，`click=1` 子集每个种子完全相同，扰动不到购买塔）——而 `_with_bands` 直接把它当门槛喂给医生，等于任何分桶差距都算病，「冷门商品学不动」「新用户不会做」判的全是噪声；现在退回 Hanley-McNeil 理论带（`noise.bucket_band`）。② `rescale()` 以前只缩全局分指标带，分桶带和合成的单指标带都不缩——医生手里两把刻度不一样的尺子，复盘官退回标量时用的还是上一档的数；现在一起缩。③ `run_session` 开跑前比对带子档位与起步档位，对不上就先缩并写进 `noise_note` → 结果表；没测过噪声带、升档重测跑挂了（缩不过去）同样留痕。这件事以前**不会报错**，只会让结论慢慢错然后传染进信任分和黑名单

- 16:24 · 成员3 · 军师现在**每个方案都要写落地草图**（不只自创的）：卡片的 how_to 是论文来的通用知识，军师写的是「这张卡怎么套到当前流水线上」（动哪些配置键/字段/新零件路径）；照抄卡片原文会被打回。工兵**两份都拿到**，冲突时以军师那份为准 —— 以前是二选一，丢哪份都少一半信息

- 16:17 · 成员3 · **深度模型训练路径落地** `harness/deep.py`：embedding 表 + epoch 循环 + ModelOp/TrainOp 加载 + 每轮验证集早停 + 最佳权重回滚。界线按 R5 划 —— 人建循环（考场），Agent 写模型（考生），所以刻意不实现任何具体模型；`modules/models/mlp.py` 只是范文。21 张卡的闸门打开了
- 16:08 · 成员2 · 导出预测按可用内存分批读目标数据：`large_25pct` 出预测（train 约986万行+public_test约1062万行）一次性整份读进 16GB 的机器被系统强制杀掉（退出码137）。拆 `_fit_predict` 成 `_fit`(只训练) + `_predict_chunk`(拿家当对一批数据出预测，不重新fit任何东西)，`predict_frame` 用 `read_in_batches` 按 psutil 测出的可用内存分批读分片、拼够预算喂一批。评分那条路复用同一套，`test_预测_跟评分走同一条路` 继续通过，没有让"两条路走岔"复发。⚠️ 只覆盖目标数据这一半，train 还是整份读，机器内存紧张时（这次实测撞到：swap 已用大半、真正空闲不到3GB）连读 train 都会 OOM，记进交接文档

- 14:24 · 成员3 · 全仓审查修 7 项：🔴 `predict` 绕过 `check_supported`（配置写 deepfm 会悄悄训个 LightGBM 交上去，交付物 #4 最要命的形态）；🔴 每轮落盘那段完全没 try —— 跑在昂贵训练之后，写失败整场挂掉；🟡 缺 libomp 时 12 个测试报红（`importorskip` 只兜 ImportError）改为跳过；🟡 升档按样本量**解析缩放**噪声带（Hanley-McNeil，不用重烧训练）；predict 自动挑最新场次 + 缺 --train 给人话；events.py 断链；README 别写死测试数

- 13:32 · 成员2 · 新增 `docs/交接文档.md`：给接力的人看的现状清单——哪些需要拍板（军师不知道执行器能力边界、predict 没做能力检查）、哪些是大坑还没填（深度模型训练路径、NISE 基线复现）、哪些是顺手小事。重点讲清楚噪声带不是量一次就永远有效的：`escalate()` 升档只重跑训练，不重测噪声带，全程用的还是起步档位那次测出来的数字——换数据规模前必须手动重跑 `agent.cli noise`

- 13:29 · 成员2 · `knowledge/symptoms.yaml` 判病门槛用实测噪声带校准：「两个任务打架」原来涨0.002/跌0.001两个门槛比实测购买AUC噪声带（0.09）小近百倍，纯抖动就能判成"打架"；其余三条（冷门商品学不动/在背题/新用户不会做）同样是一个数字管两个抖动差百倍的指标。改成优先信成绩单里当次实测的噪声带字段，旧数字降级成保底默认值，并点明这些数字是在「小份」量出来的，换规模要重新量

- 13:06 · 成员2 · web 控制台补 predict 模式（交付物 #4 导出），跟 CLI 走同一条 `predict_frame` 路径；`_with_bands()` 补「分指标」字段并同步进 `doctor.md` 第三条硬规矩——之前只改了复盘官那边，医生看到的噪声带还是合成的单一数字，只是把同一个 bug 从复盘官挪到医生身上

- 13:05 · 成员2 · `agent.cli finalize` / README 里"预测导出那段代码还没写"的过时话术，改成实际能跑的 predict 命令（带上 best_pipeline 的具体路径）

- 10:47 · 成员2 · 新增 `agent.cli predict`：训练和打分本来就在做预测，缺的只是存成文件这一步。关键是跟评分走**同一条训练路径**（`_fit_predict`），不是复制一份——避免"两条路慢慢走岔，交上去的预测跟验证时评的不是同一个模型"这种分数看着正常却查不出来的错。真实数据（public_test 217 万行）验证过

- 10:12 · 成员2 · 噪声门槛改成**分指标**：实测验证集点击正样本 8,950 个、转化正样本只有 **38** 个，两个指标抖动差一个数量级，而 `summarize()` 取 `max(两者)` 当唯一标量门槛给复盘官和 PriorLedger 用 —— 购买分的纯抖动越过低门槛被记「猜对了」白送 +0.15。顺带撞出测量方法本身的坑：换种子测出的购买分噪声带是 **0.0000**，不是它稳，是保真度抽样只抽负样本、正样本全留，`click=1` 子集每个种子完全相同，扰动不到购买塔；照单全收比不改还糟，现在测出 0 就退回 Hanley-McNeil 理论带（38 个正样本 → ±0.09），一个诚实的「现在测不出来」。实测门槛：点击 0.00106 / 购买 0.08981

- 10:03 · 成员2 · ①c 执行器兑现不了的配置**当场炸**（`check_supported`）：26 张卡里只有 3 张是纯特征卡，其余 23 张落在 模型/损失函数/训练策略，而 ModelOp/TrainOp 两类零件没有任何加载机制（grep 零命中），TrainOp 接口（按 epoch 回调 + state_dict）跟 LightGBM 结构上也对不上。照这样开跑军师有近九成概率挑中无法生效的卡，账本会被系统性毒化。另加 `UnsupportedByExecutor` 把「跑不了」和「方法不行」分开记账（前者不扣信任分，否则 ESMM/DeepFM 全被扣成低分、下一场再也不提）。真做得了的真做：早停（裁判从**训练集内部**切，不碰被评的那份 —— 否则大考会拿锁定集决定停在哪）、负采样+概率还原。**同时修掉 333c9f6 引入的 NameError**：`_build_report` 引用了 `_train_and_score` 的局部变量 `ctr_kw`/`cvr_kw`，任何一次真训练都当场崩，而 111 个测试全绿是因为没有一个走过 `_train_and_score` —— 补了端到端冒烟测试

- 09:49 · 成员2 · 锁定集路径没接进 CLI 和控制台：`RealExecutor` 早就支持 `holdout_path`，但 `_make_executor()`/`_run_agent()` 从没传下去，`final_judge()` 每次直接返回「没配锁定集」，③号修好了也没有用武之地。补 `--holdout` 参数

## 08-27

- 23:57 · 成员3 · ①b 零件终于会被加载了：`load_feature_ops` / `apply_feature_ops`（按 `impl` 指路 import、只许 modules/ 下、fit 只看训练集、新列自动进特征表）。启用了却没写 impl **直接报错** —— 以前是静默无效，工兵白挨负分。工兵提示词与 pipeline.yaml 补上 impl 约定；`run --fresh` 清掉被污染的账本

- 23:53 · 成员3 · 手查出的三个严重问题修掉两个半：② 复盘官一直拿**上上轮**的成绩单做对比（off-by-one，我写的），中间那轮的进步被算进这一次，虚高的收益还传染到信任分/黑名单/升档；③ 锁定集大考用的是**末态**配置而非最佳轮 —— 收敛条件是连续 N 轮没进步，所以末态永远不等于最佳轮，而锁定集只许读一次，装不回最佳轮就跳过大考；①a 超参数接通（`model.lightgbm.*` 以前写死，工兵改了纹丝不动）+ 白名单和护栏

- 23:31 · 成员3 · README 重写：按「从零开始的人也能读懂」组织 —— 整场流程图 + 一轮四角色接力 + 三个差异化设计（先诊断再开药 / 防自欺 / 噪声带）+ 抗崩机制 + 仓库地图 + 交付物；**补齐「局限性与改进方向」8 条**（交付物 #2 明确要求，原来是「待补」）

- 22:42 · 成员3 · 改措辞：README 和 finalize 里把「交付物 #4 是模型本身」说死。`best_report.json` 是分数记录、`best_pipeline/` 是配方，两个都不是提交物 —— 原来写成「最终提交那一版的成绩单」，容易让人以为交了它就完事了

- 22:32 · 成员3 · 每一场给一个 `run_id`：日志是追加的、轮次每场从 1 重数，两个人各跑一次会糊成 [1,2,3,1,2,3,4]，评委读到的是一团乱；快照也按场分目录
- 22:32 · 成员3 · `agent.cli finalize` 一条命令出齐提交包 → `deliverables/`（逐轮日志只取一场 + 叙事 + 结果表 + 看板 + 最佳轮次的配置和零件）；.gitignore 给 `deliverables/` 开口子 —— 交付物 #3 要求日志进仓库，但 `logs/` 依然挡着，别把演习假数据带进去

- 22:11 · 成员3 · 交付物三件套：① 人工干预记录 `agent.cli intervene`（原来 interventions 只有人读没人写，结构上只能是 0 —— 一个写死的常量当不了证据）② 每轮快照 + `agent.cli restore N`（改动是叠加的，跑完 20 轮第 5 轮那版早被盖掉，交付物 #4 交不出来）③ `narrative.md` 把一整场压成一条故事线
- 22:11 · 成员3 · 顺带修：`current_config` 每轮传的都是磁盘上那份初始文本，军师和工兵从第 2 轮起看到的流水线就是过期的；改成读执行器里真正生效的那份
- 22:10 · 成员2 · 接锁定集（R3 终于落地）：`final_judge()` 整场只许读一次、读第二次直接抛错；汇总表报泛化落差。实测 public_test 上 0.5941 比开发集 0.5576 还高，落差 -0.0365 —— 因为开发集只有 38 条转化、噪声太大

- 21:30 · 成员3 · 加「待议架」`Shelf`：军师每轮提 3 个只执行 1 个，剩下的以前直接扔、下轮从头再想一遍；现在存起来下轮摆回它面前。过期规则（卡已试过 / 目标病这轮不再报 / 上限 5 条）比存本身更重要 —— 陈旧方案会让军师照着三轮前的诊断开药

- 21:08 · 成员3 · 筛卡改按 severity 加权 —— 医生本来就给了严重度，以前只做集合求交把它扔了，0.9 的重病和 0.2 的轻病权重一样
- 21:08 · 成员3 · 同批：`symptom_resolved` 单个 → 数组，方案 targets 里每个病都必须交代 before/after，少报一个直接打回（11 张多病卡以前只有一个病被验证）
- 21:08 · 成员3 · 同批：`run_session` 加 `start_fidelity`；修控制台四处 —— 结果显示的是第 0 轮基线而非最终版、界面选的保真度不生效、体检耗时没计进 GPU 小时、噪声带没传（动了 `web/server.py`）
- 21:00 · 成员2 · 修配置放错位置（特征清单从 data 挪进 features，工兵才改得动）+ 执行器真读配置 + 数组字段安全处理；实测加交叉字段 508/509/702 点击 AUC +0.0075

- 20:35 · 成员2 · 控制台加「自主迭代」模式（四个角色全程参与，之前只会跑训练不碰 Agent）；修 Anthropic 版 max_tokens 大时必须走流式的回归

- 20:15 · 成员2 · 工兵上限提到 96k，两个 LLM 封装都加截断检测（撞上限直接报「预算不够」，不再让人被「Unterminated string」误导）
- 19:59 · 成员3 · 合上成员2 的 STUB 换真货 —— `run` 也改用真接口/真范文/真配置
- 19:58 · 成员2 · STUB 换真货（工兵改看真接口/真范文/真配置，按环节选范文）+ 建 `config/pipeline.yaml`；修 R6/R10 断链；放宽 max_tokens
- 19:55 · 成员3 · **外层循环 `run --rounds N`** 落地：跨轮传成绩单/拉黑/升档/判停/挑最佳版本；五个环节各包保险丝；日志补 metrics+patch_files（交付物#3 合规）
- 19:55 · 成员3 · 同批：`PriorLedger` 靠谱度记账 + 三根断线接回（筛卡 exclude / 调度器 tried / 军师 tried_before）
- 19:55 · 成员3 · 同批：复盘官补四道硬校验（自我申报对数字、全负不许判猜对、猜错不许加分、非猜对不许 promote）；工兵补路径穿越与配置越权
- 19:55 · 成员3 · 同批：`agent/noise.py` 噪声带（多种子实测 + Hanley-McNeil 理论值交叉验证），带子自动喂医生和复盘官；`symptoms.yaml` 未动（那份要两人一起改）
- 19:55 · 成员3 · 同批：`agent/offline.py` 假模型+假执行器，`run --offline` 不花钱演习整场、可制造事故；修 `harness/executor.py` 的 config_patch 类型 bug；补齐 requirements
- 19:30 · 成员2 · 修控制台重复渲染的竞态 bug；事件流改人话摘要 + 点击展开完整思维链（`/api/detail`）
- 18:37 · 成员2 · 支持分片数据集（选目录自动读全部 parquet）；队友的 medium_5pct（191万训练/21.8万验证）端到端跑通，58 秒
- 18:28 · 成员2 · 控制台加 Finder 文件选择（`/api/pick` 调 osascript —— 浏览器拿不到真实路径，服务器在本机上可以）
- 18:23 · 成员2 · 真执行器 `harness/executor.py` + 预检 `harness/data.py` + 网页控制台 `web/server.py`：填路径→预检亮红绿灯→真实训练出成绩单
- 18:12 · 成员2 · 看板 V0：`web/build_report.py` 把 rounds.jsonl 渲染成横版一屏便当盒页面（交付物#3 的评委可读版）；加状态栏与花费估算
- 18:02 · 成员2 · 接入 DeepSeek（`llm_deepseek.py`，流式+事件流 `agent/events.py`），真实 API 首次跑通 doctor×5 与完整一轮；实测 flash 带 `reasoning_content` 思维链
- 17:11 · 成员2 · 新增训练技巧复合卡（MC-21），保留打包但加拆解验证纪律

## 08-26

- 22:47 · 成员2 · 训练时间倍数改记账（`TimeLedger`），实测覆盖拍脑袋值；靠谱度规则化交接成员3
- 22:05 · 成员2 · 卡片库 2→25 张，补第七栏「失败信号」+ 训练类范文；详见 `方法库进度.md`
- 21:13 · 成员2 · 修正 ESMM 卡的相除/条件概率错误
- 20:48 · 成员3 · 读 NISE 源码：评估口径与基线身份两个未决问题，详见 `baseline笔记.md`；加 7 张卡骨架
- 19:19 · 成员3 · 加评审说明 —— 现状、已知弱点、待决策的设计
- 18:53 · 成员3 · Agent 大脑框架落地（四角色 + 结构化输出 + 离线可跑）
- 18:31 · 成员3 · 项目规范骨架 —— 红线清单、病名词表、卡片格式、角色接口

## 08-25

- 13:22 · 成员1 · 建仓
