# 开发日志

> 规矩：**每条一行**，格式 `日期 · 谁 · 做了什么（为什么/结论）`。
> 新的写在最上面。写不下就说明该拆成两条。详细内容进 `docs/` 下对应文档，这里只留线索。

- 08-27 · 成员2 · 真执行器 `harness/executor.py` + 预检 `harness/data.py` + 网页控制台 `web/server.py`：本地填数据路径→预检亮红绿灯→真实训练出成绩单
- 08-27 · 成员2 · 看板加底部状态栏（四阶段进度+完整报错可展开）、token 后跟花费估算；DeepSeek 价格入 PRICING 表
- 08-27 · 成员2 · 看板 V0：`web/build_report.py` 把 rounds.jsonl 渲染成横版一屏便当盒页面（交付物#3 的评委可读版）
- 08-27 · 成员2 · 接入 DeepSeek（`llm_deepseek.py`，流式+事件流），真实 API 首次跑通 doctor×5 与完整一轮；实测 flash 模型带 `reasoning_content` 思维链
- 08-27 · 成员2 · 加 `agent/events.py` 事件流（看板数据源，两进程解耦）；前端计划见 `前端页面计划.md`
- 08-27 · 成员2 · 训练时间倍数改记账（`TimeLedger`），实测覆盖拍脑袋值；靠谱度规则化交接成员3
- 08-27 · 成员2 · 卡片库 2→26 张，补第七栏「失败信号」+ 训练类范文；详见 `方法库进度.md`
- 08-26 · 成员3 · 读 NISE 源码：评估口径与基线身份两个未决问题，详见 `baseline笔记.md`
- 08-26 · 成员3 · Agent 大脑框架落地（四角色+结构化输出+离线测试）
