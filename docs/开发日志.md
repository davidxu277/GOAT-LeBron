# 开发日志

> 规矩：**每条一行**，格式 `月-日 时:分 · 谁 · 做了什么（为什么/结论）`。
> **按时间倒序，最新的在最上面**；带时分是因为一天内两人各插十几条，只写日期分不出先后。
> 时间以 git 提交时间为准（`git log --date=format:"%m-%d %H:%M"`），有疑问就以它为准重排。
> 写不下就说明该拆成两条。详细内容进 `docs/` 下对应文档，这里只留线索。

## 08-28

- 13:32 · 成员2 · 新增 `docs/交接文档.md`：给接力的人看的现状清单——哪些需要拍板（军师不知道执行器能力边界、predict 没做能力检查）、哪些是大坑还没填（深度模型训练路径、NISE 基线复现）、哪些是顺手小事。重点讲清楚噪声带不是量一次就永远有效的：`escalate()` 升档只重跑训练，不重测噪声带，全程用的还是起步档位那次测出来的数字——换数据规模前必须手动重跑 `agent.cli noise`

- 13:29 · 成员2 · `knowledge/symptoms.yaml` 判病门槛用实测噪声带校准：「两个任务打架」原来涨0.002/跌0.001两个门槛比实测购买AUC噪声带（0.09）小近百倍，纯抖动就能判成"打架"；其余三条（冷门商品学不动/在背题/新用户不会做）同样是一个数字管两个抖动差百倍的指标。改成优先信成绩单里当次实测的噪声带字段，旧数字降级成保底默认值，并点明这些数字是在「小份」量出来的，换规模要重新量

- 13:06 · 成员2 · web 控制台补 predict 模式（交付物 #4 导出），跟 CLI 走同一条 `predict_frame` 路径；`_with_bands()` 补「分指标」字段并同步进 `doctor.md` 第三条硬规矩——之前只改了复盘官那边，医生看到的噪声带还是合成的单一数字，只是把同一个 bug 从复盘官挪到医生身上

- 13:05 · 成员2 · `agent.cli finalize` / README 里"预测导出那段代码还没写"的过时话术，改成实际能跑的 predict 命令（带上 best_pipeline 的具体路径）

- 10:47 · 成员2 · 新增 `agent.cli predict`：训练和打分本来就在做预测，缺的只是存成文件这一步。关键是跟评分走**同一条训练路径**（`_fit_predict`），不是复制一份——避免"两条路慢慢走岔，交上去的预测跟验证时评的不是同一个模型"这种分数看着正常却查不出来的错。真实数据（public_test 217 万行）验证过

- 10:12 · 成员2 · 噪声门槛改成**分指标**：实测验证集点击正样本 8,950 个、转化正样本只有 **38** 个，两个指标抖动差一个数量级，而 `summarize()` 取 `max(两者)` 当唯一标量门槛给复盘官和 PriorLedger 用 —— 购买分的纯抖动越过低门槛被记「猜对了」白送 +0.15。顺带撞出测量方法本身的坑：换种子测出的购买分噪声带是 **0.0000**，不是它稳，是保真度抽样只抽负样本、正样本全留，`click=1` 子集每个种子完全相同，扰动不到购买塔；照单全收比不改还糟，现在测出 0 就退回 Hanley-McNeil 理论带（38 个正样本 → ±0.09），一个诚实的「现在测不出来」。实测门槛：点击 0.00106 / 购买 0.08981

- 10:03 · 成员2 · ①c 执行器兑现不了的配置**当场炸**（`check_supported`）：26 张卡里只有 3 张是纯特征卡，其余 23 张落在 模型/损失函数/训练策略，而 ModelOp/TrainOp 两类零件没有任何加载机制（grep 零命中），TrainOp 接口（按 epoch 回调 + state_dict）跟 LightGBM 结构上也对不上。照这样开跑军师有近九成概率挑中无法生效的卡，账本会被系统性毒化。另加 `UnsupportedByExecutor` 把「跑不了」和「方法不行」分开记账（前者不扣信任分，否则 ESMM/DeepFM 全被扣成低分、下一场再也不提）。真做得了的真做：早停（裁判从**训练集内部**切，不碰被评的那份 —— 否则大考会拿锁定集决定停在哪）、负采样+概率还原。**同时修掉 333c9f6 引入的 NameError**：`_build_report` 引用了 `_train_and_score` 的局部变量 `ctr_kw`/`cvr_kw`，任何一次真训练都当场崩，而 111 个测试全绿是因为没有一个走过 `_train_and_score` —— 补了端到端冒烟测试

- 09:49 · 成员2 · 锁定集路径没接进 CLI 和控制台：`RealExecutor` 早就支持 `holdout_path`，但 `_make_executor()`/`_run_agent()` 从没传下去，`final_judge()` 每次直接返回「没配锁定集」，③号修好了也没有用武之地。补 `--holdout` 参数

## 08-27

- 23:57 · 成员3 · ①b 零件终于会被加载了：`load_feature_ops` / `apply_feature_ops`（按 `impl` 指路 import、只许 modules/ 下、fit 只看训练集、新列自动进特征表）。启用了却没写 impl **直接报错** —— 以前是静默无效，工兵白挨负分。工兵提示词与 pipeline.yaml 补上 impl 约定；`run --fresh` 清掉被污染的账本

- 23:53 · 成员3 · 手查出的三个严重问题修掉两个半：② 复盘官一直拿**上上轮**的成绩单做对比（off-by-one，我写的），中间那轮的进步被算进这一次，虚高的收益还传染到信任分/黑名单/升档；③ 锁定集大考用的是**末态**配置而非最佳轮 —— 收敛条件是连续 N 轮没进步，所以末态永远不等于最佳轮，而锁定集只许读一次，装不回最佳轮就跳过大考；①a 超参数接通（`model.lightgbm.*` 以前写死，工兵改了纹丝不动）+ 白名单和护栏

- 23:31 · 成员3 · README 重写：按「从零开始的人也能读懂」组织 —— 整场流程图 + 一轮四角色接力 + 三个差异化设计（先诊断再开药 / 防自欺 / 噪声带）+ 抗崩机制 + 仓库地图 + 交付物；**补齐「局限性与改进方向」8 条**（交付物 #2 明确要求，原来是「待补」）

- 22:42 · 成员3 · 改措辞：README 和 finalize 里把「交付物 #4 是模型本身」说死。`best_report.json` 是分数记录、`best_pipeline/` 是配方，两个都不是提交物 —— 原来写成「最终提交那一版的成绩单」，容易让人以为交了它就完事了

- 22:32 · 成员3 · 每一场给一个 `run_id`：日志是追加的、轮次每场从 1 重数，两个人各跑一次会糊成 [1,2,3,1,2,3,4]，评委读到的是一团乱；快照也按场分目录
- 22:32 · 成员3 · `agent.cli finalize` 一条命令出齐提交包 → `deliverables/`（逐轮日志只取一场 + 叙事 + 结果表 + 看板 + 最佳轮次的配置和零件）；.gitignore 给 `deliverables/` 开口子 —— 交付物 #3 要求日志进仓库，但 `logs/` 依然挡着，别把演习假数据带进去

- 22:11 · 成员3 · 交付物三件套：① 人工干预记录 `agent.cli intervene`（原来 interventions 只有人读没人写，结构上只能是 0 —— 一个写死的常量当不了证据）② 每轮快照 + `agent.cli restore N`（改动是叠加的，跑完 20 轮第 5 轮那版早被盖掉，交付物 #4 交不出来）③ `narrative.md` 把一整场压成一条故事线
- 22:11 · 成员3 · 顺带修：`current_config` 每轮传的都是磁盘上那份初始文本，军师和工兵从第 2 轮起看到的流水线就是过期的；改成读执行器里真正生效的那份
- 22:10 · 成员2 · 接锁定集（R3 终于落地）：`final_judge()` 整场只许读一次、读第二次直接抛错；汇总表报泛化落差。实测 public_test 上 0.5941 比开发集 0.5576 还高，落差 -0.0365 —— 因为开发集只有 38 条转化、噪声太大

- 21:30 · 成员3 · 加「待议架」`Shelf`：军师每轮提 3 个只执行 1 个，剩下的以前直接扔、下轮从头再想一遍；现在存起来下轮摆回它面前。过期规则（卡已试过 / 目标病这轮不再报 / 上限 5 条）比存本身更重要 —— 陈旧方案会让军师照着三轮前的诊断开药

- 21:08 · 成员3 · 筛卡改按 severity 加权 —— 医生本来就给了严重度，以前只做集合求交把它扔了，0.9 的重病和 0.2 的轻病权重一样
- 21:08 · 成员3 · 同批：`symptom_resolved` 单个 → 数组，方案 targets 里每个病都必须交代 before/after，少报一个直接打回（11 张多病卡以前只有一个病被验证）
- 21:08 · 成员3 · 同批：`run_session` 加 `start_fidelity`；修控制台四处 —— 结果显示的是第 0 轮基线而非最终版、界面选的保真度不生效、体检耗时没计进 GPU 小时、噪声带没传（动了 `web/server.py`）
- 21:00 · 成员2 · 修配置放错位置（特征清单从 data 挪进 features，工兵才改得动）+ 执行器真读配置 + 数组字段安全处理；实测加交叉字段 508/509/702 点击 AUC +0.0075

- 20:35 · 成员2 · 控制台加「自主迭代」模式（四个角色全程参与，之前只会跑训练不碰 Agent）；修 Anthropic 版 max_tokens 大时必须走流式的回归

- 20:15 · 成员2 · 工兵上限提到 96k，两个 LLM 封装都加截断检测（撞上限直接报「预算不够」，不再让人被「Unterminated string」误导）
- 19:59 · 成员3 · 合上成员2 的 STUB 换真货 —— `run` 也改用真接口/真范文/真配置
- 19:58 · 成员2 · STUB 换真货（工兵改看真接口/真范文/真配置，按环节选范文）+ 建 `config/pipeline.yaml`；修 R6/R10 断链；放宽 max_tokens
- 19:55 · 成员3 · **外层循环 `run --rounds N`** 落地：跨轮传成绩单/拉黑/升档/判停/挑最佳版本；五个环节各包保险丝；日志补 metrics+patch_files（交付物#3 合规）
- 19:55 · 成员3 · 同批：`PriorLedger` 靠谱度记账 + 三根断线接回（筛卡 exclude / 调度器 tried / 军师 tried_before）
- 19:55 · 成员3 · 同批：复盘官补四道硬校验（自我申报对数字、全负不许判猜对、猜错不许加分、非猜对不许 promote）；工兵补路径穿越与配置越权
- 19:55 · 成员3 · 同批：`agent/noise.py` 噪声带（多种子实测 + Hanley-McNeil 理论值交叉验证），带子自动喂医生和复盘官；`symptoms.yaml` 未动（那份要两人一起改）
- 19:55 · 成员3 · 同批：`agent/offline.py` 假模型+假执行器，`run --offline` 不花钱演习整场、可制造事故；修 `harness/executor.py` 的 config_patch 类型 bug；补齐 requirements
- 19:30 · 成员2 · 修控制台重复渲染的竞态 bug；事件流改人话摘要 + 点击展开完整思维链（`/api/detail`）
- 18:37 · 成员2 · 支持分片数据集（选目录自动读全部 parquet）；队友的 medium_5pct（191万训练/21.8万验证）端到端跑通，58 秒
- 18:28 · 成员2 · 控制台加 Finder 文件选择（`/api/pick` 调 osascript —— 浏览器拿不到真实路径，服务器在本机上可以）
- 18:23 · 成员2 · 真执行器 `harness/executor.py` + 预检 `harness/data.py` + 网页控制台 `web/server.py`：填路径→预检亮红绿灯→真实训练出成绩单
- 18:12 · 成员2 · 看板 V0：`web/build_report.py` 把 rounds.jsonl 渲染成横版一屏便当盒页面（交付物#3 的评委可读版）；加状态栏与花费估算
- 18:02 · 成员2 · 接入 DeepSeek（`llm_deepseek.py`，流式+事件流 `agent/events.py`），真实 API 首次跑通 doctor×5 与完整一轮；实测 flash 带 `reasoning_content` 思维链
- 17:11 · 成员2 · 新增训练技巧复合卡（MC-21），保留打包但加拆解验证纪律

## 08-26

- 22:47 · 成员2 · 训练时间倍数改记账（`TimeLedger`），实测覆盖拍脑袋值；靠谱度规则化交接成员3
- 22:05 · 成员2 · 卡片库 2→25 张，补第七栏「失败信号」+ 训练类范文；详见 `方法库进度.md`
- 21:13 · 成员2 · 修正 ESMM 卡的相除/条件概率错误
- 20:48 · 成员3 · 读 NISE 源码：评估口径与基线身份两个未决问题，详见 `baseline笔记.md`；加 7 张卡骨架
- 19:19 · 成员3 · 加评审说明 —— 现状、已知弱点、待决策的设计
- 18:53 · 成员3 · Agent 大脑框架落地（四角色 + 结构化输出 + 离线可跑）
- 18:31 · 成员3 · 项目规范骨架 —— 红线清单、病名词表、卡片格式、角色接口

## 08-25

- 13:22 · 成员1 · 建仓
