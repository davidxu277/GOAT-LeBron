# 项目红线 · 写代码前必读

本项目是一个自主机器学习研究智能体，跑在 **KuaiRand-Pure** 上：
用户内排序（within-user ranking），标签 `long_view`，指标 GAUC + nDCG@5。
**这份文件是硬约束，不是建议。违反任何一条，代码不许合并。**

写完代码后，对照文末的自查清单逐条确认。

（比赛中途从 AliCCP 换成了 KuaiRand。旧任务的流水线已经拆掉，
比赛提交版由 tag `submission-techjam2026` 钉住。）

---

## 一、数据红线（违反会直接毁掉整轮实验）

### R1 · 标签和"曝光之后才知道"的字段永远不许进入模型输入

```
long_view    play_time_ms   is_click   is_like   is_follow   is_comment
is_forward   is_hate        is_profile_enter   profile_stay_time   comment_stay_time
```

`long_view` 就是标签，而且是从 `play_time_ms` 推出来的。其余几个是同一条曝光
**之后**才发生的行为 —— 预测时根本拿不到。把其中任何一个当特征，等于直接把答案告诉模型。

**代码强制检查**：这份名单只有一处定义（`agent/roles.py` 的 `FORBIDDEN_FIELDS`），
工兵的代码里出现任何一个直接打回。不许只靠注释提醒。

### R2 · 任何统计量只能用训练集算

出现次数、均值、目标编码、分桶边界、词表 —— 全部只能在训练集上统计，
然后套用到验证集。**读验证集来算统计量 = 作弊，分数虚高，测试集必掉。**

涉及时间的特征（用户历史、看完率）更严：训练集的每一行只能用**严格早于它那一天**的
行来算 —— 数据里只有 `date`，同一天内没有先后，同一天的行一律不许互相看。

### R3 · 测试集标签全程不许读

数据按日期切：

| split | 日期 | 用途 |
|---|---|---|
| train | 4/08 – 4/21 | 训练 |
| valid | 4/22 – 4/28 | 每轮评估、调参、诊断 |
| test  | 4/29 – 5/08 | **只在最终提交时出预测**，标签被适配层锁住 |

任何训练、调参、诊断流程读 test 的标签会当场 `PermissionError`
（`kuairand_goat_bridge/src/kuairand_bridge/dataset.py`）。不要写任何绕过它的代码。

### R4 · `video_features_statistic` 那张表不许直接用

它的统计没有按日期切开，很可能覆盖了验证期和测试期 —— 用了就是拿未来的数据。
确认口径之前一律当泄漏处理。

---

## 二、代码红线

### R5 · Agent 只许写两处

1. 在 `modules/` 下**新建**零件文件（路径守卫只认 `modules/`，带 `..` 一律拒绝）
2. 改配置里的 `features` / `model` / `train` 三棵子树

`harness/`、`agent/`、`kuairand_goat_bridge/` 下的文件 Agent 不许改动。改坏了会导致整个循环崩溃。

### R6 · 零件必须实现约定接口

不许自己发明接口。接口定义见 `modules/base.py`（FeatureOp / ModelOp / TrainOp）。
模型零件想换打分规则，实现可选的 `loss(out, batch, torch)`，训练循环会优先用它。

### R7 · 所有参数从配置读，不许硬编

包括但不限于：学习率、批量大小、embedding 维度、各种阈值、分档数、文件路径。
代码里出现魔数（写死的数字）一律打回。

### R8 · 随机种子必须固定并记录

每次运行都要设种子，并写进日志。不固定种子的实验结果不予采信。

---

## 三、实验红线

### R9 · 结论只能基于验证集

不许用训练集分数说"这个改动有效"。训练集分数只用来判断是不是在背题。

### R10 · 每次运行必须产出日志

格式见 `docs/四个角色接口.md`。缺日志的运行视为无效运行。

### R11 · 提升小于 0.0005 不算提升

一律记为"说不清"，不予采纳、不予升级到更大数据。
（KuaiRand 版的量噪声工具还没写，这个兜底门槛是拍出来的，不是在这份数据上量的。）

### R12 · 关键改动要跑 3 个种子

波动大于 0.002 的"提升"当噪声处理。

---

## 四、危险信号 —— 出现任何一条，立即停下检查

| 信号 | 几乎肯定是什么问题 |
|---|---|
| GAUC > 0.9 | 标签泄漏。官方 FM 基线的验证集 GAUC 是 0.6674 |
| 训练集与验证集主分差 > 0.15 | 严重过拟合，或数据切分错了（医生的校验器会强制拦） |
| 单步提升超过 0.02 | 大概率某个特征泄漏了标签或当天的信息，先审计再庆祝 |
| 预测行数对不上、有 NaN、或所有行同一个分 | 对齐错了或模型没学到东西，分数没有意义 |
| 某个特征重要度异常高 | 该特征泄漏了答案 |

**不许"先跑着看看"。** 出现危险信号先查清楚，否则后面所有实验都建立在错误结论上。

---

## 五、评估口径（已确定，直接调官方脚本）

| 指标 | 怎么算 |
|---|---|
| GAUC | 按用户分组的 AUC，**只统计 0 < 正例数 < 曝光数 的用户**，按正例数加权 |
| nDCG@5 | 每个用户内排前 5 的 nDCG，对所有用户平均（零正例用户记 0） |
| 主分（primary） | (GAUC + nDCG@5) / 2，排名用它 |

一律调 `kuairand_goat_bridge/official_starter_kit/evaluate.py`，**不许自己重写一遍** ——
自己实现等于造一个"跟官方差一点"的分数，到最终提交才发现对不上。

---

## 六、数据字段速查

流水线交给零件的列：

| 列 | 含义 |
|---|---|
| `date` | 日期，整数（如 20220415） |
| `user_id` / `video_id` / `author_id` | 用户 / 视频 / 作者 ID |
| `tab` | 曝光发生在哪个页面（0–14） |
| `duration_ms` / `duration_bucket` | 视频时长 / 按训练集分档后的时长 |
| `label` | 标签（就是 `long_view`）—— 只许拿来在训练集上算统计量，不许当特征 |

注意：

- 模型只吃类别特征。零件输出的连续值（看完率、目标编码）由流水线按训练集分位数
  自动分档（`model.deep.numeric_bins`），不用零件自己分
- 验证集里约 98% 的用户、全部视频在训练集里出现过 —— 冷启动主要是**低频**，不是**没见过**

---

## 七、提交代码前自查

- [ ] 特征里没有 R1 那些字段
- [ ] 所有统计量只用了训练集；涉及时间的只用了更早日期的行
- [ ] 没有读 test 的标签
- [ ] 没有修改 `harness/`、`agent/`、`kuairand_goat_bridge/` 下的文件
- [ ] 没有硬编的数字和路径
- [ ] 设置并记录了随机种子
- [ ] 产出了符合格式的日志
- [ ] 分数在合理区间（GAUC 约 0.65–0.70），没有触发危险信号
