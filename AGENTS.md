# 项目红线 · 写代码前必读

本项目是一个自主机器学习研究智能体，跑在 AliCCP 数据集上。
**这份文件是硬约束，不是建议。违反任何一条，代码不许合并。**

写完代码后，对照文末的自查清单逐条确认。

---

## 一、数据红线（违反会直接毁掉整轮实验）

### R1 · 这五个字段永远不许进入模型输入

```
sample_id   common_id   click   conversion   ctcvr
```

`ctcvr = click × conversion`。把它们中任何一个当特征，等于直接把答案告诉模型。

**代码必须强制检查**：构建特征列表后，与这五个名字求交集，非空则抛异常终止。
不许只靠注释提醒。

### R2 · 任何统计量只能用训练集算

出现次数、均值、目标编码、分桶边界、词表 —— 全部只能在训练集上统计，
然后套用到验证集。**读验证集来算统计量 = 作弊，分数虚高，测试集必掉。**

### R3 · 锁定集全程不许读

我们从训练集切出两份验证集：

- **开发集** —— 每轮都可以用，随便优化
- **锁定集** —— 全程锁死，只在最终选择提交哪一版时读一次

任何训练、调参、诊断流程读取锁定集，视为严重错误。

### R4 · 隐藏测试集我们没有

不要写任何试图访问测试集标签的代码。它不存在于本仓库。

---

## 二、代码红线

### R5 · 不许改主程序

只能做两件事：

1. 在 `modules/` 下**新建**零件文件
2. 修改 `config/` 下的配置

`harness/`、`prep/`、`agent/` 下的文件不许改动。改坏了会导致整个循环崩溃。

### R6 · 零件必须实现约定接口

不许自己发明接口。接口定义见 `modules/base.py`（三种零件的接口规范）。

### R7 · 所有参数从配置读，不许硬编

包括但不限于：学习率、批量大小、embedding 维度、各种阈值、文件路径。
代码里出现魔数（写死的数字）一律打回。

### R8 · 随机种子必须固定并记录

每次运行都要设种子，并写进日志。不固定种子的实验结果不予采信。

---

## 三、实验红线

### R9 · 结论只能基于验证集

不许用训练集分数说"这个改动有效"。

### R10 · 每次运行必须产出日志

格式见 `docs/四个角色接口.md`。缺日志的运行视为无效运行。

### R11 · 提升小于 0.0005 不算提升

一律记为"说不清"，不予采纳、不予升级到更大数据。

### R12 · 关键改动要跑 3 个种子

波动大于 0.002 的"提升"当噪声处理。

---

## 四、危险信号 —— 出现任何一条，立即停下检查

| 信号 | 几乎肯定是什么问题 |
|---|---|
| 任一 AUC > 0.9 | 标签泄漏。正常水平约 0.62 |
| 训练集与验证集分数差 > 0.15 | 严重过拟合，或数据切分错了 |
| 某个特征重要度异常高 | 该特征泄漏了答案 |
| 两种口径的购买 AUC 只算了一种 | 违反第五节，必须两种都算都记 |
| 数据里出现 click=0 且 conversion=1 | 脏数据，按预处理规则清除并报告 |

**不许"先跑着看看"。** 出现危险信号先查清楚，否则后面所有实验都建立在错误结论上。

---

## 五、评估口径（⚠️ 待确认，代码必须两种都算）

### 点击 AUC —— 已确定

| 在哪些记录上算 | 正样本 |
|---|---|
| 全部曝光 | click = 1 |

### 购买 AUC —— **没确定，不许写死**

存在两种口径，官方用哪种**还没确认**：

| 口径 | 在哪些记录上算 | 谁在用 |
|---|---|---|
| A · 仅点击 | 仅 click = 1 的记录 | ESMM 系列论文的常见报法 |
| B · 全曝光 | 全部曝光 | **官方 baseline NISE 就是这么算的** |

NISE 的实际代码（`mtl_trainer.py:424`）：

```python
cvr_score = self.evaluate_fns[0](targets[:, 0], predicts[:, 0])
#   targets[:, 0] = 全部曝光的 purchase 标签，没有筛 click == 1
```

两种口径算出来的数完全不是一回事，而排名是按 **delta vs 官方 baseline**
算的 —— 口径必须跟主办方的评测脚本一致。

**在确认之前，硬性要求**：

1. 评估范围是**配置项**，不许写死在代码里
2. **两种口径都算、都写进日志**，字段名分别是
   `购买AUC_仅点击` 和 `购买AUC_全曝光`
3. 对外报分、判断"这个改动有没有效"，一律用 `购买AUC_仅点击`，
   直到主办方口径确认为止

内部可以建模 ctcvr，但报告的两个 AUC 必须用上面写明的固定范围。

> 详见 [baseline笔记.md](docs/baseline笔记.md) 第零节。
> 确认之后回来把这一节改成"已确定"。

---

## 六、数据字段速查

原始数据是两张表，用 `common_id` 多对一关联。共 23 个特征字段：

| 组 | 字段号 |
|---|---|
| 用户属性 | 101, 121, 122, 124, 125, 126, 127, 128, 129 |
| 用户历史行为（多值带权重） | 109_14, 110_14, 127_14, 150_14 |
| 商品属性 | 205, 206, 207, 210, 216 |
| 场景 | 301 |
| 用户 × 商品交叉 | 508, 509, 702, 853 |

注意：

- `127`（消费水平）和 `127_14`（历史品牌行为）是**两个不同字段**，别弄混
- ⚠️ `129` 的归属**存疑**：上表按用户属性归类，但官方 baseline 把它当**商品**字段
  （`item_cols = ['129','205','206','207','210','216']`）。需要核对官方 schema
- 多值字段（109_14 / 110_14 / 127_14 / 150_14 / 210）**只保留最后一个值会丢信息**，
  应使用集合或序列编码
- `101` 是用户 ID，`205` 是商品 ID。`common_id` **不是**用户 ID，只是连接键

---

## 七、提交代码前自查

- [ ] 特征列表里没有 R1 那五个字段
- [ ] 所有统计量只用了训练集
- [ ] 没有读取锁定集
- [ ] 没有修改 `harness/`、`prep/`、`agent/` 下的文件
- [ ] 没有硬编的数字和路径
- [ ] 设置并记录了随机种子
- [ ] 产出了符合格式的日志
- [ ] 购买 AUC 两种口径都算了、都记了（第五节）
- [ ] 分数在合理区间（约 0.60–0.70），没有触发危险信号
